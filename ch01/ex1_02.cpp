int main()
{
    return -1;
}

/**
 * $ g++ ex1_02.cpp
 * $ ./a.out
 * $ echo $?
 * $ 255
 * 
 * exit takes only integer args in the range 0 - 255, so we entered -1 and -1 is out of range.
 * Can and sometimes will be different, depending on your operating system.
 * */
 
 